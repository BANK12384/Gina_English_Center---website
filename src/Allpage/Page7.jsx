import { useState } from "react";
import "../App.css";

export default function Contact() {
  const [count, setCount] = useState(0);
  return <section className="w-screen h-screen bg-blue-100" id="contact">
    
  </section>;
}
